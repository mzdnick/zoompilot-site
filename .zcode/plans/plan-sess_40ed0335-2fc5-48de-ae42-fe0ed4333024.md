Build 5 project skills for zoompilot-site, following the project-skill-architect → project-skill-creator pipeline.

## Step 1 — Write the manifest

Create `SKILLS-PLAN.md` at the repo root (`/Users/nick/ZCodeProject/zoompilot-site/SKILLS-PLAN.md`) in the architect's exact format: project name, generation date, skills root `.agents/skills/` (with the note that the root is movable and `.zcode/skills/` is the override location), then the 5 skills in priority order: torque-chart, wiki-sync, new-paint (high), new-release, site-copy-rules (medium). Each entry: purpose, triggers, priority, structure (SKILL.md body-only), content bullets (as proposed and approved in chat).

## Step 2 — Build the skills

Write 5 directories under `.agents/skills/`, each a body-only `SKILL.md` (no references/, scripts/, assets/ — the repo's own scripts do the work):

1. **torque-chart** — two-renderer geometry sharing (`chart-geometry.js` → `TorqueChart.astro` + `gen-wiki-chart.mjs`); authority framing (available torque, two lines, no STEER_MAX, no demand curve); shared-tail dash convention; prose-number sync map (1148/800/620/44%/~29 mph across geometry, two aria-labels, figcaption); honesty rule (schematic curves, figcaption stays); units (m/s internal, mph display, counts not Nm); `node scripts/gen-wiki-chart.mjs` after geometry changes; mobile scroll / draw-in / reduced-motion CSS touchpoints; screenshots to `.playwright-mcp/` or `/tmp` only.
2. **wiki-sync** — requires sibling `../zoompilot-wiki`; three jobs and directions (wiki→site supported cars, site→wiki changelog + chart SVG); `supported-cars.json` generated, wording fixes go in the wiki repo; "Upstream release notes" tail marker; commit in both repos; wiki `custom.css` mirrors `site.css`.
3. **new-paint** — five edit sites (`site.css` `--p*` tokens + chip classes, `paints.js`, `ZP_PAINTS` in `Base.astro`, footer band); same codes, same order; `dark:true` ↔ light-ink CSS coupling; `defaultPaint` ↔ aria-pressed chip ↔ hand-computed `theme-color`.
4. **new-release** — `ver` matches `version.h` on upstream `main` (not the date); newest entry first in `src/data/changelog.js`; `npm run stamp` (rewrites `build` export; non-blocking in CI); derived consumers (RSS feed, changelog section, wiki changelog via sync, release-watch signal); finish with `sync:wiki` + commits in both repos.
5. **site-copy-rules** — "steering motor, never rack" + the two reintroduction vectors (generated `supported-cars.json` via the wiki, stale `og.png`); claims consistency map (44%, 12 units/frame vs 10, seven bands, 0 mph, CX-5 2022–25 / CX-9 2021–23) with the files each copy lives in; canonical pinned to zoompilot.ai on purpose; minimal labels in the plot, explanation in the figcaption; cross-reference to torque-chart for chart-internal numbers.

Authoring rules: frontmatter `name` + `description` only; descriptions pushy and front-loaded with trigger words, under 1024 chars; bodies under 500 lines, imperative, real paths and command names from this repo, explain the why; prose in Simplified Technical English style per global instructions; verify each cited path/constant against the repo before writing.

## Step 3 — Commit and push

Commit only the new paths (`SKILLS-PLAN.md` + `.agents/skills/`) — the uncommitted `src/styles/site.css` stays out. Push to main (standing grant for this repo; no PR). Commit message: "Add project skills: torque chart, wiki sync, paints, releases, copy rules".

## Step 4 — Report and hand off test prompts

Report where each skill landed, that the next session loads them automatically when triggers match, and give 2–3 realistic test prompts (e.g. "the new CX-70 color needs to go on the paint wall", "openpilot 0.9.10 just released, add it", "make the torque chart show the 7 bands again" → should fire and be pushed back against by the skill's framing rules). Trigger testing needs a fresh session, so prompts are for the user to try.

Out of scope / follow-up suggested separately: moving the Cloudflare Git-integration decision record into the README.